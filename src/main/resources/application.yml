spring:
  application:
    name: flood-web

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true



  thymeleaf:
    #prefix: classpath:/static/
    suffix: .html
    mode: LEGACYHTML5
    cache: false
  resources:
    static-locations: classpath:/static/
